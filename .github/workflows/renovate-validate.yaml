name: Validate renovate config
on:
  pull_request:
    paths:
      # When renovate config changes:
      - "renovate.*"
      - ".renovate.*"
      - ".github/renovate.*"
      - scripts/check-renovate-config
      - scripts/generate-renovate-config
      - .github/workflows/renovate-validate.yaml

permissions:
  contents: none

jobs:
  renovate:
    permissions:
      contents: read
    runs-on: ubuntu-arm64-small
    timeout-minutes: 5
    steps:
      - name: Checkout Code
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          persist-credentials: false

      - name: Check drift
        run: |
          ./scripts/generate-renovate-config

          ./scripts/check-renovate-config "$GITHUB_STEP_SUMMARY"

      - name: Validate Renovate Config
        uses: grafana/shared-workflows/actions/validate-renovate-config@ee85cc9b5b1d6c880b92c77c19817f29b2338c77 # validate-renovate-config/v0.1.3
        with:
          path: .github/renovate.json
